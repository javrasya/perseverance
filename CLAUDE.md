# perseverance

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `javrasya/perseverance`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary — label strings match role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — root `CONTEXT.md` plus `docs/adr/`. See `docs/agents/domain.md`.
